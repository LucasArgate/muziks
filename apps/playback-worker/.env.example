# Trigger.dev — projeto Muziks (Development)
# Dashboard: https://cloud.trigger.dev → projeto Muziks → Environment: Development → API keys
TRIGGER_SECRET_KEY=tr_dev_xxx
TRIGGER_PROJECT_REF=proj_bhvoyepszbvxvbginzgh

# Player local (mesmo secret do apps/player/.env)
MUZIKS_PLAYER_API_URL=http://localhost:3002
PLAYBACK_WORKER_SECRET=

# Opcional — default * * * * * (a cada minuto)
# PLAYBACK_WORKER_CRON=* * * * *
