# HTTP + WebSocket
PORT=8765
HOST=0.0.0.0

# Muziks Player API (apps/player) — sem barra final
MUZIKS_PLAYER_API_URL=http://localhost:3002
PLAYBACK_WORKER_SECRET=

# Spotify (Web API no bridge; Connect via librespot)
SPOTIFY_CLIENT_ID=

# Librespot (binário no PATH ou caminho absoluto)
LIBRESPOT_BIN=librespot
LIBRESPOT_PLAYER_NAME=Muziks Bridge
LIBRESPOT_CACHE_DIR=./cache

# Opcional: iniciar librespot ao subir o bridge
LIBRESPOT_AUTOSTART=false
