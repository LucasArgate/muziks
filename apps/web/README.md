# apps/web

Aplicação do player Muziks — domínio **muziks.app** (`/{slug}` no futuro).

PWA participante: fila, votação, telão. Compartilha UI com a landing via `packages/ui`.

A pasta `app/` é o App Router do Next.js (rotas), não um “app dentro do app”.
