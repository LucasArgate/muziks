{
  "name": "@muziks/db",
  "version": "0.0.0",
  "private": true,
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo db: no build step",
    "lint": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "@muziks/spotify": "workspace:*",
    "@muziks/types": "workspace:*",
    "drizzle-orm": "^0.38.3",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@muziks/config": "workspace:*",
    "dotenv": "^17.4.2",
    "drizzle-kit": "^0.30.1",
    "typescript": "^5.7.3"
  }
}
